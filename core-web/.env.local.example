# Local API (FastAPI core)
NEXT_PUBLIC_CORE_API_URL=http://localhost:8000

# Alliance Auth (public member login link on the marketing site)
NEXT_PUBLIC_AUTH_URL=https://auth.eve-emu.com

# Optional: if the site is hosted under a subpath, set at build time (e.g. /core)
# CORE_WEB_BASE_PATH=/core
