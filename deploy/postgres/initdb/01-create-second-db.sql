-- Second database for Django (eve_emu / industry_suite). Runs once per new data volume.
-- Default name must match POSTGRES_DB_AA in root `.env.example`; if you rename it, update this file or replace with a custom init.
CREATE DATABASE eve_emu_aa OWNER eve;
